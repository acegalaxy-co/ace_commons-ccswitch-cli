# &lt;Tên nhóm/dự án&gt; — mô tả 1 dòng

> Ghi chú TĨNH của nhóm (repo/DB/stack, mục đặc thù) — phần này build-index KHÔNG đụng.

<!-- Đặt 2 marker dưới vào chỗ muốn danh sách tự-sinh. build-index sẽ chỉ thay phần GIỮA 2 marker. -->
<!-- BẮT ĐẦU TỰ SINH (build-index.mjs) — đừng sửa tay giữa 2 marker -->
<!-- HẾT TỰ SINH -->

<!--
2 KIỂU INDEX:
  (A) TỰ SINH — dán 2 marker trên + chạy `node tools/build-index.mjs "<nhóm>" --write`.
      Bảng mảnh in lại từ frontmatter `status` mỗi lần. KHÔNG sửa tay phần giữa marker.
  (B) VIẾT TAY — bỏ marker, tự liệt kê `- [Tiêu đề](ten-file.md) — mô tả`.
      Linh hoạt nhóm hơn, nhưng phải nhớ thêm dòng khi tạo mảnh mới (doctor sẽ bắt mảnh mồ côi).
-->
