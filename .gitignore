# never commit real profiles with keys — only the placeholder templates in profiles/ are tracked
*.bak
*.tmp
# guard against accidentally dropping a filled-in settings.json here
settings.json
# macOS noise
.DS_Store
